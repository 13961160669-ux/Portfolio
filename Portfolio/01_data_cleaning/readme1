## 1) 项目概述 + 默认参数

本项目用于获取美元兑人民币）日度收盘价，并进行基础清洗与计算，最后在控制台输出数据预览与描述性统计。

脚本默认参数（在代码顶部可修改）：

- `START = "2010-01-01"`
- `END = "2026-03-14"`
- `WIND_CODE = "USDCNY.EX"`
- `WIND_FIELD = "close"`
- 缓存目录：`raw/`
- 缓存文件：`raw/usdcny_USDCNY_EX_close_2010-01-01_2026-03-14.parquet`

---

## 2) 目录结构

建议结构如下：

```text
project/
  main.py                 # 你的脚本（文件名随意）
  raw/                    # 自动创建，用于存放缓存 parquet
    usdcny_....parquet
  README.md
```

---

## 3) 环境依赖

基础依赖：

- Python 3.9+（建议 3.10/3.11）
- numpy
- pandas
- pyarrow（用于读写 parquet）

如需从 Wind 直接拉取数据，还需要：

- WindPy（并且机器上已安装 Wind / 能正常连接）

安装示例（不含 WindPy）：

```bash
pip install pandas numpy pyarrow
```

> WindPy 通常需要在 Wind 环境内安装/配置，能否 `import WindPy` 取决于你的本机环境。

---

## 4) 数据格式

缓存数据为 `parquet` 文件，读取后 DataFrame 形式为：

- 索引：`date`（datetime）
- 列：`close`（数值型，收盘价）

脚本运行后会额外生成/计算：

- `log_return = log(close).diff()`（对数收益率）

清洗逻辑（简要）：

- 去重并按日期排序
- 仅保留工作日（周一到周五）
- `close <= 0` 置为缺失
- 对 `log_return` 做 3σ 异常值剔除

---

## 5) 运行方式

直接运行脚本：

```bash
python main.py
```

运行逻辑：

1. 优先从 `raw/` 读取缓存 parquet
2. 若无缓存，则尝试连接 Wind 并拉取数据
3. 拉取成功会自动保存到 `raw/` 作为缓存
4. 输出数据预览、样本量、时间区间、描述性统计等信息到控制台

---

## 6) 输出文件说明

- **主要输出：控制台打印信息**
  - Python 解释器路径
  - 缓存文件路径
  - 数据前 5 行 / 后 5 行预览
  - 样本量与时间范围
  - 清洗后工作日数量变化
  - 缺失值统计
  - `close` 与 `log_return` 的描述性统计

- **生成/使用的文件**
  - `raw/usdcny_USDCNY_EX_close_2010-01-01_2026-03-14.parquet`
    - 若本地不存在且 Wind 拉取成功，会自动生成
    - 若本地存在，则直接读取复用

---
